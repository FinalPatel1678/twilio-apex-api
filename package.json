{
  "name": "twilio-apex-api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "deploy": "twilio-run deploy --override-existing-project"
  },
  "dependencies": {
    "@twilio/runtime-handler": "1.2.0",
    "moment-timezone": "0.5.47",
    "twilio": "5.0.3"
  },
  "devDependencies": {
    "twilio-run": "^4.0.3"
  },
  "engines": {
    "node": "18"
  }
}
